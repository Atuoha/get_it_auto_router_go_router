import 'package:flutter/material.dart';

class GoRouterScreen extends StatelessWidget {
  const GoRouterScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Go Router Screen'),),
    );
  }
}
