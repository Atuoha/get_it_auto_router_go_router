import 'package:flutter/material.dart';

class AutoRouterScreen extends StatelessWidget {
  const AutoRouterScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('GetIt Screen'),),
    );
  }
}
